{% extends "base.html.twig" %}
{% block body %}
	<h1>
		{% for category in categories %}
			<a href="{{ path('category_index') }}{{category.name}}"> {{ category.name }}</a>
		{% endfor %}
	</h1>
{% endblock %}